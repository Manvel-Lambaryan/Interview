{
  "name": "url-shortener-backend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch src/main.js",
    "start": "node src/main.js",
    "test": "echo \"Define test runner in package.json\" && exit 0",
    "lint": "echo \"Define eslint in package.json\" && exit 0"
  },
  "engines": {
    "node": ">=20"
  }
}
